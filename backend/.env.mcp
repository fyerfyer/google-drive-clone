# MCP Server Environment Configuration
# ====================================================================
# This file is loaded when NODE_ENV=mcp (MCP stdio server standalone mode).
# All services use localhost instead of Docker container names.
# Docker containers (mongo, redis, minio, qdrant) must be running.
# ====================================================================

# Server
PORT=5000
NODE_ENV=mcp

# MongoDB init credentials
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=password123

# Database connection (localhost — Docker port-mapped)
MONGODB_URI=mongodb://admin:password123@localhost:27017/gdrive?authSource=admin&directConnection=true

# Security
JWT_SECRET=change_this_jwt_secret
JWT_EXPIRE=7d

# CORS
CORS_ORIGIN=http://localhost:5173

# Clear trash settings
TRASH_RETENTION_DAYS=30

# MinIO / Object Storage (localhost — Docker port-mapped)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin123
MINIO_ENDPOINT=http://localhost:9000
MINIO_PORT=9000
MINIO_PUBLIC_URL=http://localhost:9000

# Redis (localhost — Docker port-mapped)
REDIS_URL=redis://localhost:6379

# Frontend / Socket.IO
FRONTEND_URL=http://localhost:5173

# OnlyOffice Document Server
OFFICE_CALLBACK_URL=http://localhost:5000
ONLYOFFICE_URL=http://localhost:8080
ONLYOFFICE_JWT_ENABLED=false
ONLYOFFICE_JWT_SECRET=my_secret_jwt_key

# ====================================================================
# LLM Configuration (for AI Agent chat)
# ====================================================================
# Supports any OpenAI-compatible API (OpenAI, DeepSeek, Ollama, etc.)
LLM_API_KEY=sk-your-llm-key
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-chat

# ====================================================================
# Embedding Configuration (for Knowledge Layer / Semantic Search)
# ====================================================================
# If EMBEDDING_API_KEY is not set, falls back to LLM_API_KEY.
# If EMBEDDING_BASE_URL is not set, falls back to LLM_BASE_URL.
# Set these separately when your embedding provider differs from your LLM.
EMBEDDING_API_KEY=
EMBEDDING_BASE_URL=
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIMENSION=1536

# ====================================================================
# Qdrant Vector Database (for Knowledge Layer)
# ====================================================================
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=

# ====================================================================
# MCP Auto-Authentication (optional)
# ====================================================================
# Generate an API key in the Drive web UI: Settings > API Keys
# If set, the stdio server auto-authenticates on startup.
# MCP_API_KEY=gdrive_your_api_key_here
